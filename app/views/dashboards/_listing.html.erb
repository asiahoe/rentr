<li>
  <p><%= link_to (image_tag listing.image_url, width: 200), listing %></p>
  <p><%= listing.name %></p>
  <p><%= listing.price %></p>
  <p><%= link_to "Edit Listing", edit_listing_path(listing) %></p>
  <p><%= link_to "Delete Listing", listing_path(listing), method: :delete %></p>
</li>
